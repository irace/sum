# Repository Instructions

## Git Workflow

- After every completed change, validate the work, commit the intended repository
  files, and push the commit to `origin/main` before reporting completion.
- Keep secrets, local data, dependencies, and generated build output out of commits.
- If a push cannot complete, report the blocker and the unpushed commit explicitly.
