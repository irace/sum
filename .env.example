DATABASE_URL=postgres://sum:sum@localhost:5434/sum
SUM_ORIGIN=http://localhost:3003
SUM_PORT=3003
# Generate once: openssl rand -hex 32. Keep this key to decrypt saved Link tokens.
SUM_ENCRYPTION_KEY=
# Enable only behind your trusted reverse proxy.
SUM_TRUST_PROXY=false
